<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
	<h4>Title</h4>
	<%= f.text_field :title %>
	<h4>Opinion</h4>
	<%= f.text_area :body %>
	<%= f.submit "Update Book" %>
<% end %>
<span><%= link_to "Show", book_path(@book) %> | </span>
<span><%= link_to "back", books_path %></span>