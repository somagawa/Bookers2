<div class="container">
	<div class="row">
		<div class="col-lg-3">
			<%= render "books/list", user: @user, book: @newbook %>
		</div>
		<div class="col-lg-9">
			<h1>Book detail</h1>
			<table class="table table-hover">
				<tbody>
					<tr>
						<td><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg", size: '60x60' %><br>
							<%= link_to @user.name, user_path(@user) %></td>
						<td><%= @book.title %></td>
						<td><%= @book.body %></td>

					    <% if @user.id == current_user.id %>
					  		<td><%= link_to "Edit", edit_book_path(@book) %> |　</td>
							<td><%= link_to "Destroy", book_path(@book), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
						<% else %>
							<td></td>
							<td></td>
					  <% end %>
					</tr>
				</tbody>
			</table>
		</div>
	</div>
</div>